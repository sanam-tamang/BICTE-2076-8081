

#include<stdio.h>
#include<conio.h>

enum xyz{a,b,c,d};
                        /* Enumeration starts with curly braces and first value
                        is 0 by default  */
int main()

{
    int sum;
  sum=a+b+c+d;
    printf("\n Sum is %d ",sum);
  

    return 0; 
}
