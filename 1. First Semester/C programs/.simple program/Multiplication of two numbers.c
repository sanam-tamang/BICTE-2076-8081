

#include<stdio.h>
#include<conio.h>

void main()
{
    int a,b; /* a,b is variable, it is must be declare before it's used */

    printf("\n Enter two numbers ");
    scanf("%d%d",&a,&b);

    printf("The sum of %dx%d=%d ",a,b,a*xb);

    getch();

}

